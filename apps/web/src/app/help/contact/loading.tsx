import { HelpRouteLoading } from '@/components/help/help-route-loading';

export default function ContactLoading() {
  return (
    <HelpRouteLoading
      title="Contact support"
      description="Opening the support request form."
      variant="support"
    />
  );
}
