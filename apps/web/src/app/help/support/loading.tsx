import { HelpRouteLoading } from '@/components/help/help-route-loading';

export default function SupportLoading() {
  return (
    <HelpRouteLoading
      title="Support"
      description="Loading the support request form."
      variant="support"
    />
  );
}
